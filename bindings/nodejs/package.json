{
  "name": "lob-rs",
  "version": "0.1.0",
  "description": "Node.js bindings for the Rust orderbook",
  "scripts": {
    "build": "napi build --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2"
  }
}
